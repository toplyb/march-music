import { createStore } from 'vuex'
import musicModel from '@/store/modules/music'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    musicModel
  }
})
